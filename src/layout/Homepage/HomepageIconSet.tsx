import React, { FC } from "react";
import styled from "@emotion/styled";
import { colors } from "@src/vars";

const Root = styled.div`
height: 600px;
//width: 100vw;
background: ${colors.aqua};
`


const HomepageIconSet:FC = () => <Root></Root>

export default HomepageIconSet
